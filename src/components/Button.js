//rafce
const Button = (props) => {

    return (
        <div>
            <button className="btn btn-secondary" onClick={props.onClick}>My Butt</button>
        </div>
    )
}

export default Button
